// import { GET_LOCATION } from '../actions/actionTypes'

const initialState = {
  locations: {}
}

export default function (state = initialState, action) {
  switch (action.type) {
    default:
      return state
  }
}
